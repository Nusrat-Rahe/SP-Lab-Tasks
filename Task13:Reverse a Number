#include<stdio.h>
int main(){
    int num;
    int rev_num=0;
    int rem;
    scanf("%d",&num);
    while(num>0){
       rem=num%10;
       rev_num=rev_num*10+rem;
       num/=10;
       }
    printf("%d\n",rev_num);

    return 0;
   }

