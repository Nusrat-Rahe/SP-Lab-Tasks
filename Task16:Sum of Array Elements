#include<stdio.h>
int main(){
    int size;
    scanf("%d",&size);
    int arr[size],sum=0;
    for(int i=0;i<size;i++){
        scanf("%d",&arr[i]);
        sum=sum+arr[i];
    }
    printf("sum=%d\n",sum);
return 0;
}
